<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}La Nîmes'alerie, animalerie en ligne{% endblock %}</title>
    <link rel="icon"
          href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
        <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}">
        <link rel="stylesheet" href="{{ asset('css/cookie.css') }}">
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
        <script rel="script" src="{{ asset('js/bootstrap.bundle.js') }}"></script>
        <script
                rel="script"
                src="{{ asset('https://code.jquery.com/jquery-3.6.0.min.js') }}"
                integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4="
                crossorigin="anonymous"
        ></script>

    {% endblock %}
</head>
<body>
{% block body %}{% endblock %}

{% if  app.request.cookies.get('accepted') != 1 %}

        <div class="row my-5 position-absolute top-50 ms-3">
            <div class="col-md-12 col-sm-12 my-5 button-fixed">
                <div class="d-flex justify-content-center my-5">
                    <div class="card p-3 cookie"><span>Nous utilisons des cookies afin d'assurer le bon fonctionnement du site.<br></span>
                        <div class="my-4 text-right d-flex justify-content-center"><a
                                    href="{{ path('cookie_authorize') }}">
                                <button class="btn btn-success btn-sm " type="button">Accepter les cookies</button>
                            </a></div>
                    </div>
                </div>
            </div>
        </div>

{% endif %}
</body>
</html>
